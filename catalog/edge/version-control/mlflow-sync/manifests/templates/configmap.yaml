# =============================================================================
# Edge version synchronisation script (files/sync.py)
# ISO/IEC 42001: B.6.1.3.2, B.6.2.6.4, B.6.2.8.1
# =============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "edge-mlflow-sync.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "edge-mlflow-sync.labels" . | nindent 4 }}
data:
  sync.py: |
{{ .Files.Get "files/sync.py" | indent 4 }}
